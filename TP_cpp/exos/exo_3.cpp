#include <algorithm>
#include <functional>
#include <iostream>

using namespace std::placeholders;

int main()
{
  /*
  int x;
  int acc;
  std::vector<int> v {1,2,3,4,5};

  int foldl = accumulate(v.begin(),v.end(), 0, [](acc,x) {return acc + x;});
  std::cout << foldl << std::endl;

  // std::cout << accumulate(begin(v), end(v), 0, plus<int>()) << std::endl
  // std::cout << accumulate(begin(v), end(v), 0, [] (int acc, intx){return x<acc?x:acc;}) << std::endl;
  // std::cout << *max_element(begin(v), end(v))<< std::endl
  */
  return 0;
}

